import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    textStyle: {
        color: '#434343',
        fontFamily: "Shabnam",
        direction: "rtl",
        textAlign: "right"
    },
}))